import { Inter, Zen_Dots } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})

export const zen_dots = Zen_Dots({
    weight: "400",
  subsets: ['latin'],
  display: 'swap',
})